# 每日广告报告 · 使用说明

自动计算课程广告花费、Lead 数、Cost per Lead，按"场"生成每日 dashboard。
覆盖两条业务线：**Maction 连锁** 和 **品誉绩效**（含 Winfred / Wilson / Sandy 等 marketer）。

在线报告链接（已发布）：https://claude.ai/artifact/KMoMjyqrZu9nRbVPjhPEMi

---

## 一、报告每天看什么

- **今日总花费 / 今日新增 Lead / 今日综合 CPL**：当天全部活跃场次的合计（"当日"= 今日累计 − 昨日累计，系统自动算增量）
- **今日各场明细卡片**：每一场今日花费、今日 Lead、今日 CPL（红/黄/绿分级）、预算进度与余额、迷你趋势
- **今日 Cost per Lead 排名**：从便宜到贵，一眼看出哪个 marketer / 哪场效果好
- **全期趋势图**：每日总花费 + 每日新增 Lead
- **完整历史记录表**：每场每天的累计明细

CPL 颜色阈值：≤60 便宜（绿）、60–120 一般（黄）、>120 偏贵（红）。可在 `daily_ads_dashboard.html` 里搜 `CPL_GOOD` 调整。

---

## 二、两种数据来源

### 路径 A：读 Excel（现在就能用）
Marketer 照旧填 `All Meta Ads Reports.xlsx`，你双击 **`生成今日报告.bat`** → 自动出报告并打开。

### 路径 B：直接读 Meta Ads Manager（目标方案，自动化程度最高）
接通后 **花费 / Lead / 点击 / CPL 全自动**，marketer 不用手填。设置一次即可，见第三节。

> ⚠️ 无论哪条路径，「**有效 Lead vs 垃圾 Lead**」都需人工或 CRM 标记 —— Meta API 只知道"收到几个 Lead"，判断不了哪个是垃圾。这一栏保留人工填写。

---

## 三、接通 Meta API 的步骤（路径 B）

1. **建 App 拿 Token**：登录 https://developers.facebook.com → 建一个 App → 加 **Marketing API** 产品 →
   在 Business Settings 里建一个 **System User**，授予广告账户 **ads_read** 权限，生成**长期 Token**（不会过期）。
2. **设置 Token（不要写进代码）**：PowerShell 里执行
   ```powershell
   [Environment]::SetEnvironmentVariable("META_ACCESS_TOKEN","你的token","User")
   ```
3. **填账户 ID**：打开 `scripts/meta_ads_pull.py`，把 `AD_ACCOUNTS` 里的 `act_REPLACE_ME` 换成你两条业务线的真实广告账户 ID（Ads Manager 左上角可看到）。
4. **建场次对应表**：复制 `scripts/campaign_mapping.example.json` 为 `scripts/campaign_mapping.json`，
   把每个广告 campaign（用 campaign_id 最稳）对应到 line / marketer / 讲座预习日 / 预算。
5. **切换数据源**：把 `生成今日报告.bat` 里
   `python scripts\parse_excel.py ...` 那行注释掉，改用下面的 `python scripts\meta_ads_pull.py`。
6. 双击 `.bat` → 直接从 Ads Manager 出报告。

> 我可以在你拿到 Token、账户 ID、campaign 列表后，帮你把 `AD_ACCOUNTS`、`campaign_mapping.json`、
> 和 Lead 的 action 类型（`LEAD_ACTION_TYPES`）都对准你们的实际投放方式并跑通。

---

## 四、每天自动跑（可选）

用 Windows「任务计划程序」(Task Scheduler) 新建任务，每天固定时间运行 `生成今日报告.bat` 即可无人值守出报告。
需要的话我给你写好任务计划的一键导入。

---

## 五、文件清单

| 文件 | 作用 |
|---|---|
| `daily_ads_dashboard.html` | 报告本体（双击查看）|
| `生成今日报告.bat` | 一键生成 |
| `scripts/parse_excel.py` | 路径 A：解析 Excel |
| `scripts/meta_ads_pull.py` | 路径 B：拉 Meta API |
| `scripts/process.py` | 共用：算每日增量、CPL、预算余额 |
| `scripts/build_report.py` | 把数据注入报告 HTML |
| `scripts/campaign_mapping.example.json` | campaign→场次 对应表模板 |
| `dashboard_data.json` | 中间数据（每次自动重生成）|

依赖：Python 3 + `openpyxl`（`pip install openpyxl`）。Meta API 路径无需额外库。
